/**
 * Styles the board needs to own the swipe — NATIVE.
 *
 * Nothing to do: the gesture handler already has exclusive claim to touches on
 * the board, and there is no browser scrolling to suppress.
 */

import type { ViewStyle } from 'react-native';

export const boardGestureStyle: ViewStyle = {};
