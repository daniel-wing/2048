/**
 * No document to title — NATIVE.
 *
 * The task-switcher label comes from the app manifest, not from a screen, so
 * this is deliberately inert rather than mapped onto something approximate.
 */

export function useDocumentTitle(_title: string): void {
  /* Intentionally empty. */
}
